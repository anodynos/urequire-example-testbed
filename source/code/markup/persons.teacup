# No need to import any teacup keys as vars, eg `{ul, li, div} = teacup` etc
# They are automagically imported by urequire-rc-teacup-js
module.exports = (names)->
  ul ->
    li name for name in names